# Theme drop folders

Drop still wallpapers into `public/themes/free/`.
Drop live wallpaper loops (muted mp4 / webm) into `public/themes/premium/`.

Then register the file in `src/lib/themes.ts`.
Purchased live wallpapers stay owned even if the subscription lapses.
